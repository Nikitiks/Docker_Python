services:
  app:
    build: 
      context: ./core
      dockerfile: Dockerfile
    container_name: python_app
    ports:
      - "8000:8000"
    volumes:
      - books_data:/project/static/books
    restart: on-failure


volumes:
  books_data:
  